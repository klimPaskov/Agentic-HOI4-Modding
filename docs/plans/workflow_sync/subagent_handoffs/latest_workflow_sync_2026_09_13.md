# Reusable workflow sync — 2026-09-13

## Result and scope

The general starter's current skills, canonical agents, four runtime projections, instruction template, documentation, asset helpers/references, and 3D tooling were reconciled with the reusable Chaos Redux workflow.

This is a selective, mod-agnostic port, not a byte-for-byte clone. **All state ledgers are excluded**, including their skills, agents, schemas, templates, helpers, tests, generated entries, and instructions. No Chaos gameplay, feature-result assets, event catalogs, temporary audits, installed vendor trees, provider jobs, or machine-specific runtime state was imported.

The starter intentionally has no root `AGENTS.md`. `AGENTS_template.md` remains the adaptation authority installed as a destination mod's `AGENTS.md`; `AGENTS_chaos_redux.md` remains an optional example.

## Comparison baseline

- Target committed baseline: `59cc276cd79db5e69b0b39280f09c831d86a2348`, with pre-existing dirty September 8 synchronization work preserved.
- Source at intake: `5c5d3b0020a06f7fd9ac4bef8a3f40e633a2e42f`.
- Source at final review: `04c70cf601900f84646f482a81225279880da506`.
- The intervening committed diff is empty for `AGENTS.md`, `.agents/skills`, `.codex/agents`, and `.tools`.
- Comparison also included the source's dirty working-tree workflow changes and new adapter/interface-reference files. Therefore the source commit alone does not reproduce every adopted change. Source runtime reports and generated configuration are comparison evidence only, not starter deployment proof.
- Neither repository was committed or pushed by this pass. Existing target changes were not reverted, and the whole Git diff must not be attributed solely to this September 13 pass.

## Skills: adoption and disposition

All 20 actual starter skills were reviewed through the combined September 8 and September 13 passes.

| Surface | Current disposition |
| --- | --- |
| Feature assets | Keep generic source/provenance/processing ownership; adopt eligibility and requirement gates for simple category pictures, conditional image-generation capabilities, appropriate review, and concise ordinary artifact bookkeeping. Preserve security, download, dependency, uncertain-mutation, and provenance hashes. |
| Events | Adopt picture eligibility, event-owned GUI routing, matching MCP analysis, and generic country activation/lifecycle integration. The new country-activation reference is conditional on an actual destination provider API, not a mandatory Chaos framework. |
| Decisions/missions | Adopt native custom-cost and tooltip discipline, compact layouts, category-picture eligibility, matched GUI evidence, and specialized probability audits for every in-scope weighted surface. Do not import Chaos state-puzzle consumers or ledgers. |
| Focus trees | Preserve reward-scope and route validation; require the specialized probability workflow for every in-scope focus-selection weighted surface, not only unusually complex trees. |
| Planning/improvement | Adopt explicit acceptance basis and proposal/queued/accepted dispositions, requirement-to-consumer coverage, cost/picture/presentation gates, useful motion, and MCP handoffs. File location, dates, labels, and old handoffs do not establish approval. GUI rewrite remains optional; reviewed previews/comparison and defect correction remain required. |
| Frame animation | Adopt accepted seed/neighbor lineage, drift and loop-seam review, and matching MCP evidence. Generated frame sheets remain separate from skeletal actions. |
| Scripted GUI | Keep the generic live-preview and matched-comparison workflow, add actual installed reference-shelf mapping and clean-fixture/provenance/fidelity limits. Do not claim imported screenshots are fresh live evidence. |
| MTTH | Preserve the stronger generic MCP probability and safe variable/constant workflow; source formatting changes do not justify replacing it. |
| Debug/playtest | Preserve explicit invocation and generic launch/debug-parser boundaries; no automatic game launch is authorized by a workflow sync. |
| 3D model pipeline | Split the full production contract into a mandatory mode-routed production guide, source-reference policy, and motion/repair QA. Adopt separate Meshy body/firearm tasks, one approved input per geometry task, bounded Blender continuation, measured retargeting, material/normal/root/export proof, actual motion capability gates, and parent-owned runtime completion. |
| ComfyUI router and Cloud/Local/RunPod providers | Retain generic explicit provider selection, ownership, credential, cost and user-run RunPod boundaries. Do not import project-local provider state or silently switch providers. |
| Portrait production | Retain the dedicated end-to-end portrait owner's generic source, placeholder, provider, final-processing and wiring contract. |
| Super events/text-audio research | Retain source verification, licensing, ownership and runtime handoff discipline without source-local catalogs or presentation results. |
| Subagents | Preserve isolated explicit prompts, canonical-definition authority, disjoint ownership and parent integration; align cross-skill 3D routes and current evidence gates. |
| XLSX | Retain the generic helper's stronger discovery, timeout and JSON failure behavior plus provider-free tests. The dirty source helper would regress these protections, and its event-catalog contract is not reusable. |

Image model/backend requests are conditional on the actual selectable tool schema. Flare/static Sunburst, editable GUI animation and an Images 2.5 capability are not invented when the available native tool does not expose them. No provider runtime was patched to manufacture support.

The source skill prose has inherited tensions around localisation ownership versus researched quote exceptions and an ideas-only brief versus a technical implementation handoff. This sync retains the starter's bounded ownership rather than making an unrelated blanket rewrite. Uncertain or recently active tools are preserved when their owner/replacement is not established.

## Canonical agents and runtimes

All 23 canonical roles remain in `.codex/agents/*.toml`. Reusable prompt changes from the prior pass are retained; current model-policy corrections include:

- 3D pipeline: `gpt-5.6-sol / high`.
- Event UI worker: `gpt-5.6-luna / max`.
- Improvement-loop planner and feature-completion auditor: `gpt-5.6-sol / xhigh`.
- Scripted-system architect, skill maintainer and documentation curator: `gpt-5.6-sol / high`.

The event UI worker owns one named event's window, not repository-wide interfaces. Weighted logic remains the dedicated probability auditor's responsibility. No state-ledger role was added.

Claude, Cursor, OpenCode and Qoder each have 23 tracked generated projections. They were regenerated from canonical TOMLs and pass all four synchronizers' `--check` mode. Alternate runtime model selection inherits that runtime's configuration rather than pretending Codex model identifiers are universally supported. Source-local three-runtime/ignored-output prose was not imported.

## Instructions, documentation and asset tooling

The instruction template, optional example, root README, tools README and MCP integration guide now agree on explicit acceptance/disposition evidence, four-runtime tracking, picture eligibility, optional GUI rewrite, read-only technology inspect/render/compare, pinned installation and exact unavailable-route blockers.

Standalone viewer availability is distinct from MCP tool exposure and route health. A source-local absent-viewer claim is not a portable starter fact.

The prior achievement template/processor port is preserved, including eligible/grey/not-eligible output handling and generic advisor composition. Other non-3D executable asset-helper differences were reviewed and did not require replacing stronger generic behavior merely for line endings or project help text.

The current vanilla interface shelf contains 42 reviewed PNG/SVG artifacts in five actual source families. The copied source/target file sets and hashes match. Its generic README identifies full views, crops, SVGs, contact sheets and fixture limits; no stale source migration path, Chaos generated art or live-evidence claim was copied. These are construction/review references, not runtime wiring.

## 3D tools and safety boundaries

Reusable source adapter logic and 16 repair/support modules were ported or updated. They cover explicit batches and remaps, fitted/manual rigs, skin/winding/seam repair, visibility/material probes, identity-leaf and yaw guards, owned export partitions, measured retarget/root handling, action phase/channel inspection and actual-byte reimport checks.

Generic-only protections remain: nested bounded jobs, attempt namespaces, saved-normalization verification, BVH format evidence, rigid-weapon isolation/attachment and exact source/receipt containment. A final scan corrected the source-specific texture converter path and removed hardcoded source-model attribution from repair output. Declarative fitted specs record the actual bounded author; other manual tools identify their authoring route instead of claiming an unobserved model.

The adapter version is `1.6.0`, including the virtual package's lock entry. Bootstrap records the complete local Python source hash closure, rejects missing/unreviewed imports and duplicate tool names, and runtime startup rejects source-set/hash drift. Existing generated deployments must rebootstrap before use.

The exact app-owned Meshy launcher contract, pinned 0.4.0 runtime tree/integrity and checksum-locked io_pdx_mesh are retained. Chaos-owned mutable Meshy wrappers, vendor/runtime patches and generated environment locks were deliberately not transplanted.

Optional text-to-motion client support adds validated prime/swift requests, status/list/estimate handling, exclusive animation source selection, exact-format immutable downloads and redaction. It first checks actual app-owned launcher tool names and argument schemas. A contract declaration is intent, not evidence that the current launcher exposes those tools. Missing capability records an exact blocker and uses authorized Blender continuation where applicable; it does not authorize REST or a project wrapper.

Paid operations journal intent before dispatch and are not automatically replayed after uncertain results. Mutating Blender requests are likewise not replayed; bounded receipt inspection distinguishes known replies from uncertainty. The canonical Blender stdio transport keeps stdin open until its matching response and drains output safely. No arbitrary Python, shell, URL or unrestricted write surface was added.

Motion/repair QA distinguishes anatomical scale from bone-tail length, inspects interpolation/support/death/loop phases, requires measured corner-normal evidence, packs PDX glossiness as `255 - roughness`, verifies exact image/material ownership and serialized root/facing/export fields, and preserves accepted adjacent textures. A plausible viewport is not engine proof.

## Validation and publication

- All 20 skills pass `quick_validate.py`.
- All 25 Codex TOMLs parse; 23 canonical roles match all four generated projections.
- Generic script/bootstrap/manifest/sync suites: 32 tests pass.
- Feature-asset helpers: 25 tests pass.
- Spreadsheet helper: 5 tests pass using the bundled runtime with openpyxl. System Python 3.13 initially lacked that test dependency; no package install was needed.
- 3D pipeline: all 255 provider-free tests pass in the final full-suite run. Together with the other suites, 317 automated tests pass.
- All 42 copied vanilla references have matching source/target hashes and file sets.
- Generic skill, canonical-agent, adapter/tool and template scans have no Chaos namespace or state-ledger identifiers.
- Manifest schema and exact-commit evidence validation pass.
- `git diff --check` passes; Windows LF-to-CRLF notices and Pillow future-deprecation warnings are not validation failures.

Manifest capabilities and inclusion rules describe the updated workflow, while `expected_files` were regenerated honestly from committed target revision `59cc276cd79db5e69b0b39280f09c831d86a2348`. Dirty/new files are not fabricated into a published revision. **Before publishing these changes, commit the workflow files, regenerate manifest evidence against that exact new commit, and rerun the validator.**

No provider operation, package setup, Blender session, HOI4 launch, live GUI preview, live launcher motion probe or in-game validation ran. Runtime/service availability and real-feature acceptance remain job-scoped checks, not completion claims for this repository synchronization.

## Handoff precedence

This report is the current integrated synchronization status. The September 8 report and August handoffs remain historical evidence; their old runtime, model, firearm and Blender-policy descriptions are not current instructions. The actual skills, canonical TOMLs, synchronizers, template and current tool contracts are authoritative.

The separate September 13 documentation handoff records that worker's bounded edits. Its unresolved parent-owned model, skill and manifest follow-ups are disposed here, except real-runtime checks and post-commit publication evidence, which remain explicitly unperformed.
